global:
  scrape_interval: 15s
 
scrape_configs:
  - job_name: "housing-api"
    metrics_path: /metrics
    static_configs:
      - targets: ["host.docker.internal:8001"]  # API from deploy.sh (host 8001 -> container 8000)